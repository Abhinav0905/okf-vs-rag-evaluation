# WMP-CRIS evaluation harness — pinned at build time (2026-07). See
# REPRODUCIBILITY.md for the exact frozen environment (pip freeze).
pydantic>=2,<3
pyyaml>=6
numpy>=1.26
scipy>=1.11
psycopg[binary]>=3.1
pgvector>=0.2
sentence-transformers>=2.7
PyMuPDF>=1.24
tiktoken>=0.7
boto3>=1.34
# optional backends
faiss-cpu>=1.8 ; platform_system != "Darwin" or platform_machine != "arm64"
openai>=1.30
# dev / test
pytest>=8
pytest-cov>=5
